package command.filter

import command.Command
import image.{GrayscaleImage, Image}
import image.pixel.Pixel

class Rotate (degree: Int) extends Command {
  override def execute(image: GrayscaleImage) = {

  }
}
